{
  "name": "Python 3 para Análise de Dados",
  "image": "mcr.microsoft.com/devcontainers/python:3.10-bullseye",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ]
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y git-lfs && pip install -r requirements.txt"
}
